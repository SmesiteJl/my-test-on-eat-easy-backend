subprojects {
    group = 'com.technokratos.eateasy'
    version = '1.0-SNAPSHOT'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    configurations {
        configureEach {
            resolutionStrategy {
                failOnVersionConflict()
            }
        }
    }

    /*afterEvaluate { project ->
        project.tasks.matching { it.name == 'jar' }.all {
            enabled = false
        }
        project.tasks.matching { it.name == 'bootJar' }.all {
            enabled = true
        }
    }*/
}
